<div class="container mt-2">

<% if @timeslot.empty? %>
<%= @timeslot.inspect %>
    <h1>No Booking Availabe, try again later!</h1>
<% else %>
<h1>New Booking</h1>
<%= render 'form', booking: @booking %>
<% end %>

<%= link_to 'Back', restaurants_path, class:"btn btn-secondary mt-4" %>
</div>